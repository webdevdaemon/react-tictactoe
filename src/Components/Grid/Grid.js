import './Grid.css'

import React from 'react'
import PropTypes from 'prop-types'

const Grid = () => (
	<div className="Grid">
		<p> Grid </p>
	</div>
)
Grid.propTypes = {}
Grid.defaultProps = {}


export default Grid
