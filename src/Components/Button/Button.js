import './Button.css'

import React from 'react'
import PropTypes from 'prop-types'

const Button = () => (
	<div className="Button">
		<p> Button </p>
	</div>
)
Button.propTypes = {}
Button.defaultProps = {}


export default Button
